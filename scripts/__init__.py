"""Scripts package init.

This makes `scripts` importable as a module package so you can run
the test script with `python -m scripts.run_poll_once` from the project root.
"""

__all__ = ["run_poll_once"]
